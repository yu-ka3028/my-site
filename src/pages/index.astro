---
import Layout from '../layouts/Layout.astro'
import { getCollection } from 'astro:content'

const posts = (await getCollection('blog'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3)
---

<Layout title="My Site">
  <!-- Profile -->
  <section class="mb-16">
    <div class="flex items-center gap-6 mb-6">
      <div class="w-20 h-20 rounded-full bg-gray-200 flex items-center justify-center text-3xl shrink-0">
        👤
      </div>
      <div>
        <h1 class="text-2xl font-bold">Your Name</h1>
        <p class="text-gray-500 text-sm mt-1">Web Developer</p>
      </div>
    </div>
    <p class="text-gray-700 leading-relaxed">
      こんにちは。Web 開発が好きなエンジニアです。このサイトでは技術的なことや日々の学びを発信しています。
    </p>
    <div class="flex gap-4 mt-4">
      <a href="https://github.com" class="text-sm text-gray-500 hover:text-gray-900 transition-colors">
        GitHub
      </a>
      <a href="https://twitter.com" class="text-sm text-gray-500 hover:text-gray-900 transition-colors">
        X (Twitter)
      </a>
    </div>
  </section>

  <!-- Skills -->
  <section class="mb-16">
    <h2 class="text-lg font-semibold mb-4">Skills</h2>
    <div class="flex flex-wrap gap-2">
      {['TypeScript', 'React', 'Next.js', 'Astro', 'Tailwind CSS', 'Node.js'].map(skill => (
        <span class="px-3 py-1 bg-gray-100 text-gray-700 text-sm rounded-full">
          {skill}
        </span>
      ))}
    </div>
  </section>

  <!-- Recent Posts -->
  <section>
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold">Recent Posts</h2>
      <a href="/blog" class="text-sm text-gray-500 hover:text-gray-900 transition-colors">
        すべて見る →
      </a>
    </div>
    <ul class="space-y-4">
      {posts.map(post => (
        <li>
          <a href={`/blog/${post.id}`} class="group block">
            <p class="font-medium group-hover:text-gray-500 transition-colors">
              {post.data.title}
            </p>
            <p class="text-sm text-gray-400 mt-0.5">
              {post.data.pubDate.toLocaleDateString('ja-JP', { year: 'numeric', month: 'long', day: 'numeric' })}
            </p>
          </a>
        </li>
      ))}
    </ul>
  </section>
</Layout>
